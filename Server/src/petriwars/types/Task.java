package petriwars.types;


public interface Task {
	/*TASKS:
	 * 
	 * Move -> m
	 * Attack Move -> a
	 * Get Upgrade -> (whatever the upgrade hotkey is)
	 * 		flagella - >f
	 * 		cell wall ->c
	 * 		cilia	 - >e
	 * 		lysosome - >r
	 * 		chrolplast->v
	 * 		divide   - >d
	 */
	
	public void exec();
}
